 // Fichier JavaScript global de GEOCAP
// C’est le fichier qui contient le JS général du site

document.addEventListener('DOMContentLoaded', function() {
    // attend que toute la page soit complètement chargée

    console.log('GEOCAP est chargé !');
    // affiche un message dans la console (outil développeur)
    // sert à vérifier que le JavaScript fonctionne
});